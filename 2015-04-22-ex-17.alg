algoritmo "Criar um algoritmo em PORTUGOL que imprima todos os números de 1 até 100, inclusive, e a soma do quadrado desses números."

var
 n, soma : inteiro
inicio
   para n de 1 ate 100 faca
  soma <- soma+n
 escreval (n)
  fimpara
  escreval ("A soma de todos os números é:",soma*soma)
fimalgoritmo
